"""Standalone interfaces for individual services

Each module provides a minimal interactive shell to test a single service.

Usage:
    python -m lookup_vim.cli.standalone.dictionary
    python -m lookup_vim.cli.standalone.translation
    python -m lookup_vim.cli.standalone.conversation
    python -m lookup_vim.cli.standalone.lookup
"""
